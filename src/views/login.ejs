<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head') %>
<body>

    <%- include('partials/header',{q : ''}) %>

    <h1>Login</h1>
    <form method="post" action="/user/login">
        <label for="email">Usuario:</label>
        <input type="text" id="email" name="email"value="<%= locals.old && (old.email && old.email) %>">
        <p style="color: red;" class="login-grupoA"><%= locals.errors && errors.email ? errors.email.msg : '' %></p>

        <label for="password">Contraseña:</label>
        <input type="password" id="password" name="password"value="<%= locals.old && (old.password && old.password) %>">
            <p style="color: red;" class="login-grupoA"><%= locals.errors && errors.password ? errors.password.msg : '' %></p>
        <button type="submit">Registrarse</button>
    </form>
</body>
</html>
