<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <!-- Agregando Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>

<body>
    <div class="container" style="max-width: 500px;">
        <h1 class="text-center my-4">Register</h1>

        <form action="/user/register" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="firstName">Nombre:</label>
                <input type="text" id="firstName" name="firstName" class="form-control"
                    value="<%= locals.old && (old.firstName && old.firstName) %>">
                <p style="color: red;" class="login-grupoA">
                    <%= locals.errors && errors.firstName ? errors.firstName.msg : '' %>
                </p>
            </div>

            <div class="form-group">
                <label for="lastName">Apellido:</label>
                <input type="text" id="lastName" name="lastName" class="form-control"
                    value="<%= locals.old && (old.lastName && old.lastName) %>">
                <p style="color: red;" class="login-grupoA">
                    <%= locals.errors && errors.lastName ? errors.lastName.msg : '' %>
                </p>
            </div>

            <div class="form-group">
                <label for="profilePicture">Foto de perfil:</label>
                <input type="file" id="profilePicture" name="profilePicture" class="form-control">
                <p style="color: red;" class="login-grupoA">
                    <%= locals.errors && errors.profilePicture ? errors.profilePicture.msg : '' %>
                </p>
            </div>

            <div class="form-group">
                <label for="address">Dirección:</label>
                <input type="text" id="address" name="address" class="form-control"
                    value="<%= locals.old && (old.address && old.address) %>">
                <p style="color: red;" class="login-grupoA">
                    <%= locals.errors && errors.address ? errors.address.msg : '' %>
                </p>
            </div>

            <div class="form-group">
                <label for="postalCode">Código Postal:</label>
                <input type="text" id="postalCode" name="postalCode" class="form-control"
                    value="<%= locals.old && (old.postalCode && old.postalCode) %>">
                <p style="color: red;" class="login-grupoA">
                    <%= locals.errors && errors.postalCode ? errors.postalCode.msg : '' %>
                </p>
            </div>

            <div class="form-group">
                <label for="email">Correo Electrónico:</label>
                <input type="email" id="email" name="email" class="form-control"
                    value="<%= locals.old && (old.email && old.email) %>">
                <p style="color: red;" class="login-grupoA">
                    <%= locals.errors && errors.email ? errors.email.msg : '' %>
                </p>
            </div>

            <div class="form-group">
                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" class="form-control"
                    value="<%= locals.old && (old.password && old.password) %>">
                <p style="color: red;" class="login-grupoA">
                    <%= locals.errors && errors.password ? errors.password.msg : '' %>
                </p>
            </div>

            <div class="form-group">
                <label for="password2">Confirmar Contraseña:</label>
                <input type="password" id="password2" name="password2" class="form-control"
                    value="<%= locals.old && (old.password2 && old.password2) %>">
                <p style="color: red;" class="login-grupoA">
                    <%= locals.errors && errors.password2 ? errors.password2.msg : '' %>
                </p>
            </div>

            <button type="submit" class="btn btn-primary" style="background-color: purple; border: none;">Registrarse</button>
        </form>
        <a href="/user/login" class="d-block text-center my-3" style="color: purple;">Tengo una cuenta</a>
    </div>
</body>

</html>
